hibernate:
    hbm2ddl:
        auto: create-drop
    show_sql: true
openbuildright:
    reportmapper:
        objectstore:
            image:
                max-width: 1280
                max-height: 1280
                normalize-image: true
            minio:
                bucket: dev
                access-key: minio_root_user_mock
                secret-key: minio_root_password_mock
                endpoint: http://localhost:1234
server:
    port: 8081
spring:
  security:
    oauth2:
      client:
        registration:
          keycloak:
            provider: keycloak
            clientId: mapper
            clientSecret: OY81t9AOADA66Jm8NFaEJcnkCCdPP9ff
        provider:
          keycloak:
            issuer-uri:  "http://localhost:9003/realms/master"
            authorization-uri:  "http://localhost:9003/realms/master/protocol/openid-connect/auth"
            token-uri:  "http://localhost:9003/realms/master/protocol/openid-connect/token"
            user-info-uri:  "http://localhost:9003/realms/master/protocol/openid-connect/userinfo"
            jwk-set-uri: http://localhost:9003/realms/master/protocol/openid-connect/certs

    application:
        name: map-report-backend
    datasource:
        driverClassName: org.h2.Driver
        password: sa
        url: jdbc:h2:mem:myDb;DB_CLOSE_DELAY=-1
        username: sa
    data:
        mongodb:
            host: localhost
            port: 1234
            username: root_mock
            password: example_mock
            database: admin
